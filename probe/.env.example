# 공공데이터포털에서 발급받은 서비스키.
#
# 이 파일은 템플릿이다. 여기에 키를 적지 말 것 — 이 파일은 커밋된다.
# 반드시 probe/.env 로 복사한 뒤, 복사본에만 키를 넣는다. (.env는 .gitignore로 제외됨)
#
#   Copy-Item probe\.env.example probe\.env
#
# 포털 마이페이지에 '인코딩'과 '디코딩' 두 벌이 보이는데 어느 쪽이든 된다.
# 스크립트가 '%' 포함 여부로 판별해서 알아서 처리한다.
KAC_SERVICE_KEY=
